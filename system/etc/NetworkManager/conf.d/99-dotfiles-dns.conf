# Tell NetworkManager not to write /etc/resolv.conf — the local
# dnscrypt-proxy resolver owns it (pointed at 127.0.0.1 / ::1).
#
# Ports nixcfg modules/nixos/networking.nix: networking.networkmanager.dns = "none".

[main]
dns=none
systemd-resolved=false
